## 0.3.0

- Move the Flutter adapter into the symmetric `packages/flutter` monorepo path.
- Preserve the existing `andura_ui` library and public component APIs.
- Include 151 design-system presets with one generated Dart file per system.
- Maintain parity with React, Compose, and SwiftUI adapters.

## 0.2.0

- Add contextual design-system themes and expanded shared components.

## 0.1.0

- Initial reusable Flutter foundations, themes, and components.
